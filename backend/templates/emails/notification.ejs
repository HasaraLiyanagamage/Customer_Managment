<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 1px solid #eee;
      margin-bottom: 20px;
    }
    .logo {
      max-width: 200px;
      height: auto;
    }
    .content {
      padding: 20px 0;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #4CAF50;
      color: white !important;
      text-decoration: none;
      border-radius: 4px;
      margin: 20px 0;
    }
    .notification {
      background-color: #f9f9f9;
      padding: 20px;
      border-radius: 4px;
      margin: 15px 0;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      font-size: 12px;
      color: #777;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= appName %></h1>
  </div>
  
  <div class="content">
    <h2><%= title %></h2>
    
    <div class="notification">
      <%= message %>
    </div>
    
    <% if (action && action.url && action.text) { %>
      <p style="text-align: center;">
        <a href="<%= action.url %>" class="button"><%= action.text %></a>
      </p>
    <% } %>
    
    <p>If you have any questions, please don't hesitate to contact our support team.</p>
    
    <p>Best regards,<br>The <%= appName %> Team</p>
  </div>
  
  <div class="footer">
    <p>© <%= year %> <%= appName %>. All rights reserved.</p>
    <p>You're receiving this email because you have an account with <%= appName %>.</p>
    <p><a href="<%= appUrl %>/preferences">Update notification preferences</a> | <a href="<%= appUrl %>/unsubscribe">Unsubscribe</a></p>
  </div>
</body>
</html>
